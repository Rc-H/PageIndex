You are given one JSON object describing a document section and a partial part of the document. Your task is to check whether that section starts in the partial document.

The provided text contains tags like <physical_index_X> and <physical_index_X> to indicate the physical location of the page X.

Important rules:
- Treat tags like <physical_index_X> as the only source of truth for physical page numbers.
- Ignore page numbers printed in the document body, header, or footer when determining physical page location.
- Printed page labels may be logical/document page numbers and may differ from the PDF's physical page index.
- When returning "physical_index", return the tagged physical page where the section starts, not the printed page number shown on the page.

If the full target section starts in the partial given document, set "physical_index" to "<physical_index_X>".

If the full target section does not start in the partial given document, set "physical_index" to null.

Return exactly one JSON object in the following format:
{{
    "structure": "1.2",
    "title": "Section title",
    "physical_index": "<physical_index_12>(keep the format)"
}}

Do not return a JSON array.
Do not wrap the object in another object.
Do not add explanations.
Keep "structure" and "title" unchanged from the given section object.
